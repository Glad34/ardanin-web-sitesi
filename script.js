{'filename': 'script.js', 'code': 'document.addEventListener(\'DOMContentLoaded\', function() {\n    const typewriterElement = document.getElementById(\'typewriter\');\n    const text = "Arda İlci, İzmir\'deki Gayrimenkul Danışmanınız";\n    let index = 0;\n\n    function type() {\n        if (index < text.length) {\n            typewriterElement.innerHTML += text.charAt(index);\n            index++;\n            setTimeout(type, 100); // Yazma hızı (ms)\n        }\n    }\n\n    // Sadece geniş ekranlarda daktilo efektini çalıştır\n    if (window.innerWidth > 768) {\n        if (typewriterElement) {\n            type();\n        } \n    } else {\n        // Mobil ekranda daktilo efekti olmadan tüm metni direkt göster\n        if (typewriterElement) {\n            typewriterElement.innerHTML = text;\n        }\n    }\n});\n'}